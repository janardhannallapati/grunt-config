/**
 * Build Modernizr
 */
'use strict';

var config = require('../config');

module.exports = {


          // Jasmine testing framework configuration options
       all: {
                options: {
                    specs: 'test/spec/{,*/}*.js'
                }
            }
        

	
    
};
